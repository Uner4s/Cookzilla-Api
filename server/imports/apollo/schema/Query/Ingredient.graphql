type Query {
  #return all Ingredients
  ingredients: [Ingredient]
  showIngredient (_id: ID!): Ingredient
  pendingIngredients: [Ingredient]
  getAcceptedIngredients: [Ingredient]
  getNameIngredient (search: String): [Ingredient]
}
