import Implements from 'server/imports/collections/Implements'
export default function (root, params, {userId}) {
  Implements.remove(params)
// Const es una variable que no puede cambiar, no se puede sobre escribir.
  return Boolean
}
